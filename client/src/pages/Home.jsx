import Hero from '@components/Hero';
import HomeFound from '@components/HomeFound';
const Home = () => {
  

  return (
    <div>
      <Hero />
      <HomeFound />
    </div>
  )
};

export default Home;
